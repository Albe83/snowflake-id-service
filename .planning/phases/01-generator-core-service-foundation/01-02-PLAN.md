---
phase: 01-generator-core-service-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - internal/idgen/generator.go
  - internal/idgen/generator_test.go
  - internal/config/config.go
  - internal/config/config_test.go
  - cmd/snowflake-service/main.go
  - README.md
autonomous: true
requirements:
  - GEN-03
  - OPS-05
must_haves:
  truths:
    - "Decode reconstructs the absolute millisecond timestamp, node ID, and sequence from every generated positive signed int64 ID."
    - "Concurrent generation from many goroutines has zero duplicate IDs under the race detector."
    - "After 4096 IDs at one mocked millisecond, generation waits for the next mocked millisecond and returns a unique ID."
    - "A backwards clock returns the typed error and ID zero without changing high-water state; a subsequent call rechecks the clock."
    - "README gives an operator a correct local run and failure contract, including explicit NODE_ID, PORT, fixed epoch, string IDs, and the approximately-2095 horizon."
  artifacts:
    - "internal/idgen/generator_test.go contains deterministic round-trip, positivity, skew, frozen-clock overflow, and concurrent uniqueness tests."
    - "README.md documents the Phase 1 executable contract and explicitly excludes Phase 2/3 functionality."
  key_links:
    - "The controllable test clock can advance while Generate is spinning, proving sequence exhaustion does not wrap or return a duplicate."
    - "The skew test observes unchanged generator state through the next successful generated ID and repeated clock check."
    - "README commands match the NODE_ID/PORT names and cmd/snowflake-service path implemented by plan 01."
---

<objective>
Turn the tracer into a provable uniqueness-critical kernel and an explicit operator-facing Phase 1 foundation. This expansion covers the five roadmap success criteria with deterministic tests and documents the locked contracts without adding deferred API, drain, Kubernetes, or metrics work.

Purpose: The core value is uniqueness, so correctness must be demonstrated under concurrency, sequence exhaustion, positive signed-64-bit boundaries, and backwards-clock rejection rather than inferred from a smoke route.
Output: Complete idgen/config test coverage and a README that makes the fixed, explicit configuration model operationally unambiguous.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-generator-core-service-foundation/01-CONTEXT.md
@.planning/phases/01-generator-core-service-foundation/01-RESEARCH.md
@.planning/phases/01-generator-core-service-foundation/01-PATTERNS.md
@.planning/phases/01-generator-core-service-foundation/01-01-PLAN.md
@/home/albe/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/albe/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Prove layout, skew, overflow, and concurrent uniqueness</name>
  <files>internal/idgen/generator.go, internal/idgen/generator_test.go</files>
  <read_first>internal/idgen/layout.go, internal/idgen/generator.go, internal/config/config.go, .planning/phases/01-generator-core-service-foundation/01-RESEARCH.md, .planning/phases/01-generator-core-service-foundation/01-PATTERNS.md</read_first>
  <behavior>
    - Test 1: NewGenerator with a valid node and clock yields a positive ID; Decode returns the expected absolute timestamp, node ID, and sequence zero on the first call.
    - Test 2: A controllable clock held at one millisecond supports 4096 unique IDs, then the 4097th call blocks until the clock advances and returns sequence zero at the new millisecond.
    - Test 3: Moving the clock behind the recorded millisecond returns errors.Is(err, ErrClockMovedBackwards), ID zero, and leaves the next successful ID anchored at the prior high-water mark after the clock recovers.
    - Test 4: Multiple goroutines generating a substantial ID set produce no duplicates and complete under go test -race.
    - Test 5: Invalid constructor node IDs and a pre-epoch constructor clock return errors without creating a usable generator.
  </behavior>
  <action>Implement or refine the injected clock seam in internal/idgen/generator.go so tests can deterministically freeze and advance time without sleeps. Add generator_test.go with a mutex-protected controllable clock, table-driven constructor cases, a Decode round trip, positivity assertion, a 4096-plus-one overflow test that releases the waiting call by advancing the clock, a backwards-clock test that checks ID zero and unchanged state behavior, and an N-goroutine uniqueness test collecting IDs in a synchronized map. Keep the critical section around clock comparison, sequence selection, overflow wait, and lastMs update; do not replace the mutex with atomics or channels. If implementation changes are needed, retain the typed error contract from D-01, the no-mutation rule from D-02, the fixed epoch from D-05/D-06, and the spin-wait choice from D-03. Run tests first as red where practical, then implement until green, and avoid timing-based flakiness.
  <documentation_requirement>README.md must explicitly document the selected reject-with-typed-error clock-skew policy per D-01/D-02, that rejection leaves generator state unchanged, the rejected bounded-wait and monotonic-anchored alternatives with their trade-offs per D-03, and that Phase 2 maps the typed error to HTTP 503. Do not implement or document that 503 mapping as Phase 1 temporary-route behavior.</documentation_requirement>
  <verify>
    <automated>go test -race ./internal/idgen</automated>
    <automated>go vet ./...</automated>
  </verify>
  <acceptance_criteria>Race-enabled tests prove zero duplicates across concurrent callers; the frozen-clock test proves the 4097th ID advances rather than wraps; skew returns the typed error with no state mutation; Decode and positivity tests prove GEN-03; constructor validation covers OPS-05's generator-side clock and node guards.</acceptance_criteria>
  <done>internal/idgen/generator_test.go passes repeatedly with go test -race ./internal/idgen, and all generator state transitions remain synchronized and deterministic.</done>
</task>

<task type="auto">
  <name>Task 2: Publish the Phase 1 runbook and final validation contract</name>
  <files>internal/config/config.go, internal/config/config_test.go, cmd/snowflake-service/main.go, README.md</files>
  <read_first>internal/config/config.go, internal/config/config_test.go, cmd/snowflake-service/main.go, internal/idgen/layout.go, .planning/phases/01-generator-core-service-foundation/01-CONTEXT.md, .planning/ROADMAP.md</read_first>
  <action>Review the config and composition-root implementation against D-07, D-09, D-10, D-11, D-12, D-13, and D-16; make only necessary corrections so Load reads environment once, the default PORT is 8080, the pre-epoch check happens before ListenAndServe, the JSON boot log includes node_id and epoch, the temporary route is explicitly marked for Phase 2 replacement, and signal shutdown is bounded. Extend config_test.go if any matrix case from plan 01 is absent. Create README.md with the exact local command NODE_ID=0 go run ./cmd/snowflake-service, optional PORT usage, curl example for GET /dev/id, quoted-string response contract, node range 0..1023, fixed epoch 2026-01-01T00:00:00Z / 1767225600000, approximately-2095 validity warning, intentional non-zero startup failure for invalid config or pre-epoch clock, and the stdlib-only/race-test commands. State that NODE_ID is explicit in every environment and do not describe POD_NAME, ordinal resolution, Kubernetes manifests, POST /v1/ids, health endpoints, readiness drain, or metrics as available Phase 1 behavior. Cite the locked decisions in implementation comments or documentation where useful, without adding an EPOCH environment variable.
  <verify>
    <automated>go test -race ./... &amp;&amp; go vet ./...</automated>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>README instructions run the implemented binary and accurately describe the fixed epoch, explicit NODE_ID, PORT default, string JSON dev response, fail-fast validation, shutdown skeleton, and approximately-2095 horizon. README explicitly documents the selected reject-with-typed-error clock-skew policy, no state mutation on rejection, the rejected bounded-wait and monotonic-anchored alternatives with their trade-offs, and that Phase 2 maps the typed error to HTTP 503. The full standard-library repository passes race tests and vet with no Phase 2/3 or v2 artifacts. The D-14/D-15 explicit-node deployment boundary is stated without creating deployment artifacts.</acceptance_criteria>
  <done>All five Phase 1 roadmap success criteria are covered by executable tests or the documented boot contract, GEN-03 and OPS-05 remain traceable, and the repository is ready for Phase 2 without misleading operational instructions.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Concurrent callers → Generator | Untrusted scheduling and clock observations exercise shared uniqueness state. |
| Operator environment → startup validation | Deployment configuration determines identity, port, and whether the process is allowed to mint IDs. |
| Documentation → operator actions | README commands and warnings influence production configuration choices. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-01 | Tampering | Generator sequence/high-water state | critical | mitigate | Race-enabled concurrent uniqueness test plus mutex-protected implementation and frozen-clock overflow proof. |
| T-02-02 | Repudiation | Clock-skew behavior | high | mitigate | Typed ErrClockMovedBackwards contract, no-mutation test, and explicit README failure semantics. |
| T-02-03 | Denial of service | Sequence exhaustion spin | medium | mitigate | Controllable-clock test proves the wait terminates at the next millisecond and avoids sequence wrapping. |
| T-02-04 | Tampering | Operator configuration | high | mitigate | Config matrix tests and README require explicit NODE_ID and document the valid range and fail-fast behavior. |
| T-02-SC | Tampering | Go module/dependency installs | high | mitigate | Retain stdlib-only go.mod and run go vet plus race-enabled tests as the dependency and correctness gate. |
</threat_model>

<verification>
Run `go test -race ./...`, `go vet ./...`, and `git diff --check`. Inspect the test suite for the five roadmap success criteria: Decode/layout, concurrent uniqueness, 4097th frozen-clock generation, invalid boot configuration, and typed reject-with-no-state-mutation clock skew. Run the documented curl smoke test against the binary as the final manual check.
</verification>

<success_criteria>
Phase 1 is complete when the generator's positive signed-64-bit 41/10/12 IDs are round-trip decoded, concurrency and sequence exhaustion are race-tested, startup validation fails fast for invalid node or pre-epoch clock, and the reject-with-typed-error clock policy is documented and tested. The binary remains deliberately limited to the temporary /dev/id route and shutdown skeleton.
</success_criteria>

## Artifacts this phase produces

- `internal/idgen/generator_test.go` — layout, positivity, skew, overflow, constructor, and concurrent uniqueness proof.
- `internal/config/config_test.go` — environment and startup validation matrix.
- `README.md` — local operation, fixed epoch, explicit identity, string contract, and Phase 1 boundary documentation.

<output>Create `.planning/phases/01-generator-core-service-foundation/01-02-SUMMARY.md` when execution completes.</output>
